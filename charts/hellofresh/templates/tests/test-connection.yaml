apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hellofresh.fullname" . }}-test-connection"
  labels:
    {{- include "hellofresh.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hellofresh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
